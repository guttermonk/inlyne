#!/usr/bin/env bash
# Direnv configuration for Nix flake development environment
#
# To use this, install direnv and run:
#   direnv allow
#
# This will automatically load the development environment when you enter this directory

use flake

# Optional: Show that the environment is loaded
export PS1="[inlyne-dev] $PS1"

# Optional: Set any project-specific environment variables here
# export INLYNE_LOG=debug